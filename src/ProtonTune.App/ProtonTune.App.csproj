<Project Sdk="Microsoft.NET.Sdk.Razor">

    <PropertyGroup>
        <OutputType>Exe</OutputType>
        <TargetFramework>net10.0</TargetFramework>
        <ImplicitUsings>enable</ImplicitUsings>
        <Nullable>enable</Nullable>
    </PropertyGroup>

    <ItemGroup>
      <PackageReference Include="Microsoft.Extensions.Logging.Console" Version="10.0.10" />
      <PackageReference Include="PhotinoX.Blazor" Version="4.3.2" />
    </ItemGroup>

    <ItemGroup>
      <!-- These two carry data rather than code: the DLSS libraries and the setting definitions,
           each laid out beside the application by being referenced. -->
      <ProjectReference Include="..\ProtonTune.Lib.DLSS\ProtonTune.Lib.DLSS.csproj" />
      <ProjectReference Include="..\ProtonTune.GameConfiguration\ProtonTune.GameConfiguration.csproj" />
      <ProjectReference Include="..\ProtonTune.Services\ProtonTune.Services.csproj" />
      <ProjectReference Include="..\ProtonTune.UI\ProtonTune.UI.csproj" />
    </ItemGroup>

</Project>
