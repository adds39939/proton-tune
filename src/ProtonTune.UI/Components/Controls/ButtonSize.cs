namespace ProtonTune.UI.Components.Controls;

/// <summary>
/// How much room a button takes.
/// </summary>
public enum ButtonSize
{
    /// <summary>The usual size, for a screen's own actions.</summary>
    Medium,

    /// <summary>For a button repeated down a list, where the row sets the height.</summary>
    Small
}
